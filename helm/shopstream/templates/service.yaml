apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopstream.fullname" . }}
  labels:
    {{- include "shopstream.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "shopstream.selectorLabels" . | nindent 4 }}
